1.服务端 需要保存 客户端socket 和昵称，以及对应关系
2. 客户端 UI界面 登录，按钮事件响应，连接socket
3. 客户端 需要线程 来读取server的消息

4.客户端 连接server成功后，第一件事情：把自己的昵称发过去
  接下来，就相当于进入了大厅
  发言发消息给server

  问题：server如何知道客户端 当前发的某个消息是在干啥
  客户端 发给server 首先先发送一个标识，代表某种含义
  1：发给服务器我的昵称 1：Tom
  2：我的发言 2： 大家好 大厅聊天
  然后 再继续发送消息

  TCP，UDP 网络通信协议
  定义协议
5.server端 读取线程 首先读取的就是消息类型
    readInt(),然后再根据读到的数值，判断消息类型 1 登录 2发言
    多个客户端登录，发送昵称给server
    server 收到消息 拿到客户端昵称和socket存入map结构

6.有人进入大厅 需要广播通知所有人
server要把当前大厅所有人昵称广播一遍

7.客户端 发消息
client客户端 server服务端
c ---> s(服务端收到后，广播发给每个房间内的客户端)
客户端收到服务器的广播后(判断类型)